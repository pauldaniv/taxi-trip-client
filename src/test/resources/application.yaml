
services:
  facade-api:
    url: ${FACADE_API_HOST:http://localhost:8081}

event.file:
  localBasePath: src/test/resources
  file-key: test_events.csv
  eventFileLocal: true

aws.region: ${AWS_REGION:us-east-2}
